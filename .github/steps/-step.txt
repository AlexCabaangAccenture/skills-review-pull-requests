var forceStagingFailure =
    Environment.GetEnvironmentVariable("ForceDiaStagingFailure") == "true";

if (forceStagingFailure)
{
    throw new InvalidOperationException("Temporary DEV validation: forced DIA staging save failure.");
}
