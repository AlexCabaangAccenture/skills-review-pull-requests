exceptions
| where timestamp > ago(30m)
| where operation_Name contains "DiaIngestionFunction" or cloud_RoleName contains "deceasedestate"
| order by timestamp desc
| project timestamp, operation_Name, type, outerMessage, innermostMessage, problemId
