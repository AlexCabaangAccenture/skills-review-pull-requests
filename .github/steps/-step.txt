var recipients = SplitRecipientEmails(recipientEmails);

if (recipients.Count == 0)
{
    _logger.LogWarning(
        "DIA notification email creation skipped because no valid recipient emails were found. LogId: {LogId}, RecipientConfigKey: {RecipientConfigKey}.",
        logId,
        recipientConfigKey);

    return;
}
