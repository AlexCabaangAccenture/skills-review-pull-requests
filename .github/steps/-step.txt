Assert.Contains("DIA Dataverse staging save failed after retry exhaustion", exception.Message);
Assert.Contains(payload.FileName, exception.Message);
Assert.Equal(stagingException, exception.InnerException);
