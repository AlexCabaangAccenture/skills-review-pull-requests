catch (Exception ex)
{
    await _diaExceptionNotificationService.NotifyFailureAsync(
        payload.FileName,
        "DecryptionFailure",
        ex,
        cancellationToken);

    throw new InvalidOperationException(
        $"DIA decryption failed for file {payload.FileName}.",
        ex);
}
